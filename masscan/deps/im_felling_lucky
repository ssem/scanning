#!/bin/bash

home=`pwd`

### unzip ###
sudo apt-get install unzip

### libpcap ###
sudo apt-get install libpcap0.8-dev

### masscan ###
unzip masscan-master.zip
cd masscan-master
make
sudo make install
cd $home
rm -r masscan-master
